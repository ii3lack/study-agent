[project]
name = "study-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.2.2",
    "rich>=15.0.0",
    "zai-sdk>=0.2.3",
]

[tool.black]
line-length = 88
target-version = ["py311"]
